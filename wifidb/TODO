optimize all the code, and i mean all of it
rewrite code to OO
    daemons [import(), export(), scheduling(), stats()]
    web front end [web()]
re-design some of the pages [ap fetch, list all users, index, scheduling]
cache all exports, until new import.
finish implementing memcached
rewrite SQL methods with PDO
rewrite installer for PostgreSQL (maybe Oracle and MSSQL if I have the will power)
i have a lot of work to do